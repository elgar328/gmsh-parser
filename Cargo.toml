[package]
name = "gmsh-parser"
version = "0.1.0"
edition = "2021"

[dependencies]
thiserror = "2.0"
miette = { version = "7", features = ["fancy"] }

[dev-dependencies]
criterion = "0.5"
tempfile = "3.0"
